SELECT
    SUM(case_count)
FROM (
    SELECT
        regionres,
        COUNT(casecode) AS case_count
    FROM covid_fact
    JOIN dim_region AS dimreg
        ON dimreg.region_id = covid_fact.region_id
    GROUP BY regionres
) t;

SELECT *
FROM dim_region;